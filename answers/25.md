```sql
SELECT DISTINCT maker
FROM Product
JOIN PC ON Product.model = PC.model
WHERE PC.speed = (
    SELECT MAX(speed)
    FROM PC
    WHERE ram = (SELECT MIN(ram) FROM PC)
)
AND type = 'Printer';
```